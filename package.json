{
  "name": "mary-blooms",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "sass --watch src/scss/main.scss:css/main.css",
    "build": "sass src/scss/main.scss:css/main.css --style=compressed",
    "start": "npx serve .",
    "webhook:set": "node scripts/set-webhook.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.3",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}
